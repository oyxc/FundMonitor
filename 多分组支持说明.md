# 多分组支持功能实现说明

## 功能概述

将基金的分组标识从单个 `groupId` 改为数组 `groupIds`，实现一个基金可以同时存在于多个分组中的功能。

## 已实现的功能

### 1. FMFund.h
- 将 `@property (nonatomic, copy) NSString *groupId;` 改为 `@property (nonatomic, strong) NSArray<NSString *> *groupIds;`
- 支持基金同时属于多个分组

### 2. FMFund.m
- `encodeWithCoder:` 方法：序列化 `groupIds`
- `initWithCoder:` 方法：
  - 优先读取新的 `groupIds` 数组
  - 兼容旧数据：如果存在旧的 `groupId`，自动转换为 `groupIds` 数组
  - 如果都不存在，初始化为空数组

### 3. FMDataManager.h
```objc
// 从指定分组中移除基金（基金仍在其他分组中）
- (BOOL)removeFund:(NSString *)fundCode fromGroup:(NSString *)groupId;
```

### 4. FMDataManager.m

- `getFundsInGroup:` - 使用 `ANY groupIds == %@` 谓词查询
- `addSelectedFund:toGroup:` - 添加基金到分组
- `moveFund:toGroup:` - 将基金移动到新分组
- `removeGroup:` - 删除分组时处理基金
- `removeFund:fromGroup:` - 从指定分组移除基金

## 数据兼容性

### 向后兼容
- 旧版本数据使用 `groupId` (NSString)
- 新版本在读取时会自动将 `groupId` 转换为 `groupIds` 数组

## 使用示例

### 添加基金到多个分组
```objc
FMFund *fund = ...;
[[FMDataManager sharedManager] addSelectedFund:fund toGroup:@"group1"];
[[FMDataManager sharedManager] addSelectedFund:fund toGroup:@"group2"];
// 现在基金同时存在于 group1, group2 两个分组中
```

### 从特定分组移除基金
```objc
// 从 group2 中移除，但仍保留在 group1 中
[[FMDataManager sharedManager] removeFund:fundCode fromGroup:@"group2"];
```

---

**修改完成时间**：2026-02
**修改类型**：功能增强 - 支持多分组
**向后兼容**：是
